<p id="notice"><%= notice %></p>


		<% @posts.reverse.each do |post| %>
     <% if post.picture.present? %>
  <div class="col-md-4">
  <img src="<%= post.picture.url %>">
	</div>
      <% end %>
		<% end %>
		
