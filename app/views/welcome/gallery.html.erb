<p id="notice"><%= notice %></p>


		<% @posts.reverse.each do |post| %>
     <% if post.image.present? %>
  <div class="col-md-4">
  <img src="<%= post.image.url %>">
	</div>
      <% end %>
		<% end %>
		
