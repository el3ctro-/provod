<%= form_for(@thumb) do |f| %>
  <% if @thumb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@thumb.errors.count, "error") %> prohibited this thumb from being saved:</h2>

      <ul>
      <% @thumb.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :comment_id %><br>
    <%= f.number_field :comment_id %>
  </div>
  <div class="field">
    <%= f.label :up_or_down %><br>
    <%= f.number_field :up_or_down %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
